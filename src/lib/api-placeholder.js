// Placeholder API functions for chat and sandbox execution
// These would normally make backend API calls

export async function placeholderChatAPI({ messages, template, model, config }) {
  // Placeholder for chat API
  console.log('Chat API placeholder called with:', { messages, template, model, config });
  
  // Return a mock fragment response
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve({
        commentary: 'This is a placeholder AI response. The actual backend would generate code based on your prompt.',
        template: 'code-interpreter-v1',
        title: 'Placeholder Fragment',
        description: 'This is a placeholder fragment generated by the frontend.',
        additional_dependencies: [],
        has_additional_dependencies: false,
        install_dependencies_command: '',
        port: null,
        file_path: 'script.py',
        code: '# Placeholder code\n# Backend integration needed\nprint("Hello from Gamepac Super Agent!")\nprint("This is a placeholder response.")',
      });
    }, 1000);
  });
}

export async function placeholderSandboxAPI({ fragment }) {
  // Placeholder for sandbox execution
  console.log('Sandbox execution placeholder called with:', fragment);
  
  return new Promise((resolve) => {
    setTimeout(() => {
      if (fragment.template === 'code-interpreter-v1') {
        resolve({
          sbxId: 'placeholder-sandbox-id',
          template: fragment.template,
          stdout: ['Placeholder execution output', 'Backend integration needed for actual code execution'],
          stderr: [],
          cellResults: [
            {
              text: 'Execution result placeholder',
            },
          ],
        });
      } else {
        resolve({
          sbxId: 'placeholder-sandbox-id',
          template: fragment.template,
          url: 'http://localhost:3000',
        });
      }
    }, 1500);
  });
}
