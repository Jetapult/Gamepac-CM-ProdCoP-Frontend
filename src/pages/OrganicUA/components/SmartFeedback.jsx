import Assistant from "../../../components/Assistant";

const SmartFeedback = ({studio_slug}) => {
  return <div className="">
    <Assistant studio_slug={studio_slug} />
  </div>;
};

export default SmartFeedback;
